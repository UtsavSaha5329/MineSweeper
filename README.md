# MineSweeper
A Java program that generates a Minesweeper-style grid based on user-defined mine positions. The program allows the user to specify mine locations and then calculates the number of adjacent mines for every non-mine cell.
