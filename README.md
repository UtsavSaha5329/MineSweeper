# MineSweeper

A Java program that generates a Minesweeper-style grid based on user-defined mine positions.

# Concepts Used

* 2D arrays
* Nested loops
* 8-directional neighbour traversal
* Boundary checking
* Arrays.fill()
* Character arrays

The program allows the user to specify mine locations and then calculates the number of adjacent mines for every non-mine cell.

